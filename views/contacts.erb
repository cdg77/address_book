<h1>Here are all the contacts:</h1>

<ul>
  <% @contacts.each() do |contact| %>
    <li><a href="/contacts/
      <%= contact.id() %>">
      <%= contact.first_name() %>
      <%= contact.last_name() %>
      <%= contact.birthday() %></a>
    </li>
    <% end %>
</ul>
